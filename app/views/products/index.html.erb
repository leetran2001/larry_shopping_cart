<p id="notice"><%= notice %></p>

<h1 class="text-center">Products</h1>

<div class="row">
  <% @products.each do |product|  %>
    <div class="card col-sm-3 text-center p-3">
      <img class="card-img-top img-fluid" src="<%= product.photo %>" alt="Card image cap">
      <div class="card-body">
        <h5 class="card-title"><%= product.name %></h5>
        <h6 class="card-title">$<%= product.price %></h6>
        <%= link_to 'Add to Cart', product_orders_path(product), class: 'btn btn-primary', method: :post %>
      </div>
    </div>
  <% end %>
</div>


<%#= link_to 'Show', product, class:"badge badge-success" %>
<%#= link_to 'Edit', edit_product_path(product) %>
<%#= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %>


<%#= link_to 'New Product', new_product_path, class:"badge badge-primary" %>
